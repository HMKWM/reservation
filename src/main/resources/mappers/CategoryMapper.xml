<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.connect.reservation.dao.CategoryDao">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into category(name) values (#{name})
    </insert>
    <select id="findById" resultType="Category">
        select id, name from category where id = #{id}
    </select>
    <select id="findAll" resultType="Category">
        select id, name from category
    </select>
    <update id="update">
        update category
        set name = #{name}
        where id = #{id}
    </update>
    <delete id="delete">
        delete from category where id = #{id}
    </delete>
    <select id="count" resultType="Integer">
        select count(*) from category
    </select>
    <select id="getCategoryList" resultType="ApiCategory">
        select c.id, c.name, count(d.id) as size
        from category c inner join product p inner join display_info d
        on p.id = d.product_id
        where c.id = p.category_id
        group by p.category_id;
    </select>
</mapper>